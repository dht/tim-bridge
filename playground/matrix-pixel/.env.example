# Copy to `.env` (this file is ignored by git) and customize as needed.
#
# If you use the vendored library without installing it into the venv:
PYTHONPATH=python3-idotmatrix-library
#
# Optional: set a platform-specific BLE identifier (macOS typically reports a UUID,
# Linux/Raspberry Pi typically reports a MAC like AA:BB:CC:DD:EE:FF).
#
# IDOTMATRIX_ADDRESS_DARWIN=548D5EE8-4BEB-6B78-E532-6E44368D78AD
# IDOTMATRIX_ADDRESS_LINUX=AA:BB:CC:DD:EE:FF
#
# Optional: pixel board size for `animation-simple.py` (16 or 32):
# IDOTMATRIX_SIZE=32
#
# Optional: used by `discover.py` to filter scanned results:
# IDOTMATRIX_NAME_SUBSTR=IDM
